To run the program, open up a windows command prompt and naviate to the root directory 
of the project.

From there run the command 
java -jar target\keyboard-0.0.1-SNAPSHOT.jar

To do a new build run
mvn clean install

The classes in the program have the ability to constantly learn. Using the train() method, input 
can constantly be written to the program. 

The method 
